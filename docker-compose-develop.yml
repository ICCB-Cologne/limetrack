version: "3.7"

services:
  saturn3sample-django:
    volumes:
      - ./logs:/logs
      - ./backend:/backend
      - ./manage.py:/manage.py:ro
      - ./requirements.txt:/requirements.txt
      - ./env:/env